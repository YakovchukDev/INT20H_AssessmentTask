import {SxProps, Theme} from "@mui/material";

export const container:SxProps<Theme>  = {
    display: "flex",
    flexDirection: "column",
}
export const variant:SxProps<Theme> = {
    display: "flex",
    width: "100%",
    padding : '5px',
}
export const variantInput:SxProps<Theme> = {
    flexGrow: 1,
}